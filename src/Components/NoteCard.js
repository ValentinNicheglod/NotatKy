import React from 'react'

const NoteCard = ({title, content, date, tags, collections}) => {
    return (
        <div className="card note-card">
            A
        </div>
    )
}

export default NoteCard
